
public class Point {
	int x, y;
	public Point(int x, int y) {
		this.x = x;
		this.y = y;
	}
	public void equals(Point x) {	
		if (x instanceof Point)
			System.out.println(this.x + "is equal to " + x);
		else
			System.out.println(this.x + "is not equal to " + x);
	}
}
